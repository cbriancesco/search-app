div(ng-show='showContent')
    //- div(class="button", ngf-select, ng-model="file", name="file", ngf-pattern="'image/*'", ngf-accept="'image/*'", ngf-max-size="20MB") Select

    //- img(ng-show='newTeam.imageShow || imageDefault', ng-src="{{newTeam.imageShow || imageDefault}}", width='200')


    p Create New Team
    input(type='text', ng-model='newTeam.name', placeholder='Team Name')

    p Team Positions
    textarea(ng-model='positions', placeholder='Team Positions')

    //- ul
    //-     li(ng-repeat='pos in newTeam.positions') {{pos}}


    button.submit__button(ng-click="createTeam()") Create Team

